@startuml langgraph_orchestration
' DROPPED: too similar to intent_routing.puml + validation_retry.puml
' No unique decision logic added.
' The full StateGraph topology is already covered:
'   - intent_routing:    validate_input → load_long_term_memory → classify_intent →
'                        enrich_context → dispatch_agents → merge_responses →
'                        verify_citations → validate_output
'   - validation_retry:  validate_output → pass/partial_pass/fail/validator_error →
'                        prepare_retry loop → fallback → update_memory →
'                        write_long_term_memory → summarize_history → audit_log → END
' The back-edge and memory/summarize branch are both shown in those two diagrams.
@enduml
